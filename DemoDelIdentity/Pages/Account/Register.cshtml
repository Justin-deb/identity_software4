@page
@using DemoIdentity.Identity.Account;
@model DemoIdentity.Identity.Account.RegisterModel
@{
}

<div>
    <form asp-route-returnUrl="@Model.ReturnUrl" method="post">

        <div>
            <label>Email</label>
            <input asp-for="Register.Email" />
        </div>

        <div>
            <label>Password</label>
            <input asp-for="Register.Password" type="password"/>
        </div>

        <div>
            <label>Verificar Password</label>
            <input asp-for="Register.Password2" type="password" />
        </div>

        <div>
            <label>Cédula</label>
            <input asp-for="Register.Cedula" />
        </div>


        <button type="submit"> Registrar</button>
    </form>

</div>